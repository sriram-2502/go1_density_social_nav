# Analysis Notes

The analysis workflow converts paired `markers_*.bag` and `vel_*.bag` files into social-navigation plots. The current controller bags provide command-integrated robot path estimates; true robot pose should be recorded in timestamped pose bags for publication-quality trajectory metrics.
