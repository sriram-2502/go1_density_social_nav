# Docker Metadata

Place small, redacted metadata files here, such as:

```text
object_detection_history_full.txt
object_detection_inspect_redacted.json
docker_apt_manual.txt
docker_pip_freeze.txt
```

Do not commit Docker image tar files.
